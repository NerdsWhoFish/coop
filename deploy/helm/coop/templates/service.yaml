apiVersion: v1
kind: Service
metadata:
  name: {{ include "coop.fullname" . }}
  labels:
    {{- include "coop.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "coop.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
